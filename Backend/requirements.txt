# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
Flask>=2.0.0
Flask-Cors>=3.0.0
pydantic[email]>=2.0.0
python-multipart>=0.0.9

# ── NLP / ML (Ai_Module/NLP) ───────────────────────────────────────────────────
scikit-learn>=1.4.0

# ── Vision (Ai_Module/vision) ─────────────────────────────────────────────────
opencv-python>=4.9.0
numpy>=1.26.0
tensorflow>=2.16.0

# ── LLM providers (optional — set API keys in environment) ────────────────────
anthropic>=0.25.0
openai>=1.14.0
google-genai>=1.0.0
python-dotenv>=1.0.0

# ── Azure Speech Services (TTS) ────────────────────────────────────────────────
azure-cognitiveservices-speech>=1.40.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0          # FastAPI TestClient dependency
